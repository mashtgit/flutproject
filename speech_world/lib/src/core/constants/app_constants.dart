class AppConstants {
  static const String appName = 'Speech World';
  static const String appVersion = '1.0.0';
  static const Duration defaultTimeout = Duration(seconds: 30);
  static const int maxRetryAttempts = 3;
}